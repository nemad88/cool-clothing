import styled from "styled-components";

export const BurgerStyled = styled.div`
  position: relative;
  display: none;
  cursor: pointer;
  margin: 2rem;
`;

export const BurgerSpanStyled = styled.span`
  padding: 0.2rem 2rem;
  background: black;
  margin: 0.5rem 0;
  display: block;
  transition: all 0.5s ease;
`;
